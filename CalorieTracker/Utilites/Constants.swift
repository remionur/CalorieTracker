import Foundation

struct Constants {
    struct Firestore {
        static let usersCollection = "users"
        static let mealsCollection = "meals"
    }
    
    struct Storage {
        static let mealImagesFolder = "meal_images"
    }
}